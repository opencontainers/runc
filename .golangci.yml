# For documentation, see https://golangci-lint.run/usage/configuration/

run:
  build-tags:
    - seccomp

linters:
  enable:
    - gofumpt
    - errorlint
    - nolintlint
    - unconvert
    - unparam

linters-settings:
  govet:
    enable:
      - nilness
