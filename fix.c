在 C89 标准中，变量声明必须位于块的开头，不允许在 `for` 循环的初始化部分直接声明变量。为了兼容旧版编译器并遵循安全编码规范，我们需要将循环变量声明移至块作用域的顶部。

以下是修复后的安全代码片段（以 `nsexec.c` 中典型的循环为例）：

**修复前（C99风格，行552附近）：**
```c
for (int i = 0; i < num_fds; i++) {
    close(fds[i]);
}
```

**修复后（C89兼容）：**
```c
int i;
for (i = 0; i < num_fds; i++) {
    close(fds[i]);
}
```

如果原循环变量声明为其他类型（如 `size_t`），也一并移出：
```c
size_t j;
for (j = 0; j < count; j++) {
    process(j);
}
```

### 安全性说明
- **无 UAF/栈溢出/污点传播风险**：仅移动变量声明位置，不改变程序逻辑与内存操作。
- **兼容性提升**：确保代码可在仅支持 C89 的编译器（如某些嵌入式工具链或旧版 GCC）下正确编译。
- **逻辑不变**：循环变量的作用域从 `for` 块扩展至外层块，但语义和生命周期与原来保持一致，不会引入新的 bugs。

若需进一步加固，建议在声明时立即初始化（如 `int i = 0;`），避免使用未初始化变量的隐患。但原 `for` 循环初始化已在循环头完成赋值，故移出后直接声明即可。